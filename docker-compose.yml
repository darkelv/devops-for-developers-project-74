services:
  app:
    image: node:20
    working_dir: /app
    user: node
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    command: sh -c "npm install && npm run build && npm run dev"
